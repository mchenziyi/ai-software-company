[ARTIFACT]
type: PRD
version: 5
status: draft
changes: |
  1. CSRF Token 生命周期闭环：增加 GET /api/csrf 端点用于页面刷新后重新获取 Token；明确 Token 存储于内存变量，刷新后通过调用该端点重新获取。
  2. Token 存储方案：Access Token 存内存，Refresh Token 存入 HttpOnly Cookie；实现 401 拦截器自动调用 /api/auth/refresh 刷新 Access Token。
  3. 补充验收标准 4.7（并发收藏测试）的操作步骤和预期结果：明确同时发起 5 个请求、数据库最终 favorited=1 且唯一约束不冲突。
  4. 细化分片上传初始凭证定义：初始凭证（uploadToken）用于授权创建任务并绑定用户和 taskId，与 signPart 凭证（每个分片的上传签名）生命周期分离；signPart 凭证每次上传前必须重新获取。
  5. WebSocket 鉴权方式改为连接后第一条消息携带 JWT 鉴权，避免 URL 参数泄露；鉴权失败时服务端主动关闭连接并发送错误码。
  6. 收藏 toggle 数据库实现：使用 favorited 字段（0/1），在 (user_id, video_id) 上建唯一索引，通过 INSERT ... ON DUPLICATE KEY UPDATE favorited = NOT favorited 实现。
  7. 搜索安全校验改为强制使用参数化查询 + 字符白名单（仅允许字母、数字、中文、下划线、减号、空格） + 长度限制（≤100 字符），移除黑名单过滤 SQL 注入关键字的表述。
  8. 热度排序衰减计算明确推荐使用定时任务（如每5分钟）预计算热力值并存入缓存（如 Redis），查询时直接读取缓存，避免实时计算性能问题。
  9. 补充收藏失败状态处理策略：采用响应后更新 UI（失败时回滚）并显示错误提示（Toast），或乐观更新 + 失败时回滚。
  10. 明确上传重试按钮行为：调用 GET /api/upload/{taskId}/parts 查询已上传分片，仅补传缺失分片，而非重新 init。
  11. 定义排序切换时的页码重置行为：切换排序后前端立即重置 page=1 并重新发起请求。
  12. 增加前端文件选择校验：在选择文件后立即检查格式（mp4/webm/mov）和大小（≤500MB），不符合则弹出提示并阻止提交。
  13. 分页接口每页数量增加最大值限制（1-100），超出时服务端返回参数错误（HTTP 400）或自动截断为 100。
  14. 明确分片续传时凭证获取策略：每个分片每次上传前必须重新调用 signPart 获取凭证，不得复用之前凭证。
  15. 搜索输入框增加前端基本校验：长度限制≤100 字符、去除首尾空格、禁止提交仅空格字符串。
  16. 补充大量验收标准（4.8~4.30+），覆盖：视频上传（格式异常、大小超限、标题/标签异常、重试、超时、UI状态）、搜索（安全校验正面/负面、空搜索、分页超限、排序切换）、WebSocket（连接失败、心跳丢失、重连、降级）、Token 刷新、未登录用户行为、响应式布局等核心场景的 happy path、异常和边界。
  17. 细化 CSRF 防护验收标准（原 4.4）：拆分为多条，包含正向用例（携带合法 Token 返回 200）及三种负向测试（不携带、错误 Token、篡改），并明确预期结果。
  18. 增加分页超限验收标准（4.10）：请求大于总页数的页码，验证响应包含 isLastPage:true 且数据为最后一页。
  19. 增加搜索安全校验测试用例（4.11 等）：SQL 注入、超长、纯空格、HTML 标签等输入，验证服务端正确处理。

---

## 1. 功能目标
建立一个基于 AI 主题的视频分享平台，允许注册用户上传、发现、浏览和收藏 AI 相关视频内容，构建一个连接 AI 爱好者和创作者的社区。

## 2. 用户故事
| 角色 | 行为 | 期望结果 |
|------|------|---------|
| 注册用户 | 上传一个新的 AI 视频 | 视频成功上传，并展示在视频列表中，标题、描述和标签信息准确。 |
| 注册用户 | 在视频列表中通过关键词搜索 | 能够快速找到标题、描述或标签中包含该关键词的视频。 |
| 注册用户 | 浏览视频列表 | 能够看到按发布时间（默认）或热度排序的视频，并可以翻页查看。 |
| 注册用户 | 收藏一个喜欢的视频 | 该视频被添加到我的收藏列表中，并且收藏状态实时更新（如心形图标变红）。 |
| 注册用户 | 查看我的收藏列表 | 能够看到所有我收藏过的视频列表，并可以取消收藏。 |
| 未登录用户 | 访问平台 | 首页可浏览视频列表，但收藏按钮需提示登录或跳转登录页；直接访问上传/收藏页面会被重定向到登录页，且登录后携带redirect参数回跳。 |

## 3. 功能点
### 3.1 用户注册与登录
- **描述**：提供注册新用户和登录现有用户的功能。所有核心操作（上传、收藏）均需要身份认证。
- **输入**：
    - 注册：用户名、邮箱、密码
    - 登录：邮箱、密码
- **输出**：
    - 成功：跳转到视频列表首页或携带的redirect页面，生成并返回 JWT 令牌，并通过 Authorization Bearer 传递。
    - 失败：展示具体错误信息（如“邮箱已注册”、“密码错误”）。
- **令牌机制**：
    - 类型：JWT
    - 过期时间：24小时（Access Token），